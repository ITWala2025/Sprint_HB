const config = {
  plugins: {
    // Tailwind CSS v4 PostCSS plugin (works with the `@import "tailwindcss"`
    // + `@theme` tokens declared in src/css/global.css)
    "@tailwindcss/postcss": {},
  },
};

export default config;